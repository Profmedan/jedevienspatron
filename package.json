{
  "name": "jedevienspatron-saas",
  "version": "0.1.0",
  "private": true,
  "description": "Jeu sérieux de comptabilité générale française — Pierre Médan",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspace=packages/game-engine",
    "build": "npm run build --workspace=packages/game-engine",
    "dev:web": "npm run dev --workspace=apps/web",
    "simulate-codex": "npx tsx scripts/simulate-codex.ts"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "lightningcss": "1.32.0",
    "lightningcss-darwin-x64": "^1.32.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
